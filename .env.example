# Database
DATABASE_URL="postgresql://username:password@localhost:5432/trichome_db?schema=public"
DIRECT_URL="postgresql://username:password@localhost:5432/trichome_db?schema=public"

NODE_ENV=development

NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET=""

# Public site URL - used for canonical links, sitemap, robots.txt, and JSON-LD
NEXT_PUBLIC_SITE_URL="https://www.trichomesshop.com"

# Google OAuth
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
NEXT_PUBLIC_GOOGLE_CLIENT_ID=""

ADMIN_PASSWORD=""

# Cloudinary
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# Paystack API Credentials
# Get these from your Paystack dashboard: https://dashboard.paystack.com/#/settings/developer
# Use pk_test_xxx / sk_test_xxx for test mode, pk_live_xxx / sk_live_xxx for production
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=""
PAYSTACK_SECRET_KEY=""

# Email (SMTP)
SMTP_SERVER=""
SMTP_PORT=587
LOGIN=""
SMTP_PASSWORD=""
EMAIL_FROM=""

# Comma-separated list of emails to notify when a new order is placed
# (in addition to all ADMIN/STAFF user accounts, which are notified automatically)
ADMIN_NOTIFICATION_EMAIL=""

NEXT_PUBLIC_WHATSAPP_NUMBER=""

# Shipbubble Shipping API — optional. If SHIPBUBBLE_API_KEY is unset, checkout
# falls back to the static per-state rate table in src/lib/shipping/calculate-shipping.ts.
# Get the API key from https://app.shipbubble.com/ dashboard > API Keys.
SHIPBUBBLE_API_KEY=""
# One-time setup values specific to this Shipbubble account (see docs/changelog
# for how to obtain them) — not something the app computes at request time.
SHIPBUBBLE_SENDER_ADDRESS_CODE=""
SHIPBUBBLE_CATEGORY_ID=""
